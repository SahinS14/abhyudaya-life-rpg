PORT=3000
JWT_SECRET=replace-this-with-a-long-random-secret-before-deploying
NODE_ENV=development
# Optional: use a persistent disk location in a production service.
# DATABASE_PATH=/var/data/abhyudaya.db
